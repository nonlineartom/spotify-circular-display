# Required when the LAN HTTPS ingress is active. The kiosk already connects to
# localhost, so no browser functionality requires Waitress on a LAN socket.
[Service]
Environment=DISPLAY_HOST=127.0.0.1
